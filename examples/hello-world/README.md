# hello-world

A tiny SDK example that runs one prompt through Shannon and prints the assistant
text.

## Requirements

- [Bun](https://bun.sh)
- `claude` on `PATH`, with a working Claude Code login
- `tmux` on `PATH`

## Run

```sh
bun install
bun run start
```

You should see `hello` printed once.
