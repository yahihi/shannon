# @humanlayer/shannon-agent-sdk

Claude Agent SDK-compatible facade for Shannon.

This package currently re-exports Shannon's implemented SDK surface from
`@humanlayer/shannon`. Install `@humanlayer/shannon` alongside it.

It is not full Claude Agent SDK parity yet; see the repo root
`GOAL_PROGRESS.md` for the current parity matrix.
